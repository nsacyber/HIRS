/**
 * This package contains a set of bean classes which encapsulate
 * a subset of the data persisted via objects in the
 * hirs.data.persist package.
 *
 * Hibernate can utilize these beans to transform the result set
 * of a query into an object.
 *
 * These beans are not intended to be persisted themselves.
 */
package hirs.data.bean;
